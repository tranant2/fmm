#ifndef QUAD_TREE
#define QUAD_TREE

#include "QTNode.hpp"
#include <iostream>
#include <vector>
#include <cmath>
#include <complex>
#include <random>

/* Convert data into cpoint objects
*/
void data_to_cpoint(double &x, double &y, std::vector<CPoint> &points);

#endif  //QUAD_TREE